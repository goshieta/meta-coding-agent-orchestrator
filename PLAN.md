# 開発オーケストレーション中間層（Meta-Agent Orchestrator）最終仕様書

## 概要

仕様書（md）を**ワンライナーで渡すだけで**、コンテナ内でCrewAI製マネジメント層がPIAgent（pi）のセッション・コンテクスト・品質を自律管理し、人間の介入なしに最終成果物をGitHubリポジトリとして完成投稿まで行う開発オーケストレーション中間層。

## ゴールと成功指標

*   **放置完了**：仕様書入力→GitHub公開まで人間介入ゼロ
    
*   **ワンライナー起動**：`run-spec.sh <spec.md> [existing_repo/]`
    
*   **コンテナ隔離**：全エージェント・piプロセスをコンテナ内で稼働（ホスト非汚染）
    
*   **品質担保**：独立QAクリティック通過のみ完了へ
    
*   **費用最適化**：管理=コスパ・検証=高機能のモデル割当。全割当を外部設定化
    
*   **新規・既存両対応**
    

## ペルソナ

*   **GoshiEta**（個人開発者・PIAgent使用者・Arch Linux・コンテナ運用あり）
    
*   **（参考）受託クライアント**：最終成果物のGitHub納品を期待
    

## 機能一覧（MoSCoW）

### Must

| ID | 機能 |
| --- | --- |
| F-01 | ワンライナー起動（CLI入口） |
| F-02 | コンテナ隔離実行 |
| F-03 | コンテクスト構築（仕様書起点） |
| F-04 | 計画・タスク分解 |
| F-05 | 実装ループ（pi操作・レポート回収・commit） |
| F-06 | 品質保証（独立QA） |
| F-07 | 完了・納品（一回push） |
| F-08 | 人間インターフェース（質問提出・強制停止） |

### Should

| ID | 機能 |
| --- | --- |
| F-09 | 設定管理（環境変数/引数の外部化） |
| F-10 | 既存案件の現状把握（調査PIAgent） |
| F-11 | GitHubへの成果物アップロード |
| F-12 | 役割別モデル割当（外部設定） |

### Could

*   専門化3-4層化・ピアレビュー細分化
    

### Won't

*   pi内蔵サブエージェント/MCP/プランモード/バックグラウンドbashに依存（外部で実装）
    

## 機能詳細

### F-01 ワンライナー起動

*   入力：`run-spec.sh <spec.md> [existing_repo/]`、オプションフラグ
    
*   処理：仕様書検証→コンテナ起動→開発パイプライン開始
    
*   出力：コンテナID・ログパス
    
*   エラー：仕様書未指定/ファイルなし→即終了
    
*   優先度：Must
    

### F-02 コンテナ隔離実行

*   全エージェント（piプロセス含む）をコンテナ内で稼働。ホスト非汚染
    
*   環境変数（モデル割当・GitHubトークン等）を起動時のみ注入
    
*   冪等性：再起動でも状態（セッション・ログ）を保持
    

### F-03 コンテクスト構築

*   新規：仕様書をpi初期プロンプトとして投入（`pi @spec.md "..."`）
    
*   既存：仕様書＋調査エージェントがpiプロセス経由で現状把握→現状レポート作成
    
*   AGENTS.md/システムプロンプト活用
    

### F-04 計画・タスク分解

*   オーケストレータが仕様書と現状からタスク分解・依存関係・優先度を設計
    
*   出力：共有ボード（`plan.md`）＋タスク別コンテクスト設計
    

### F-05 実装ループ

*   実行インターフェーサーがpiを起動・操作：`--fork`/`-c`/`--session`/`--mode rpc`
    
*   進行中の指示注入（steering/follow-up）
    
*   自然言語レポート回収→オーケストレータへ
    
*   必要に応じ `/compact` でコンテクスト解放
    
*   **タスク完了ごとにcommit**（過程を追跡）
    

### F-06 品質保証

*   QAクリティック（独立コンテクスト・批判的視点・高機能モデル）が成果物検証
    
*   問題→F-05へ再送、通過→F-07へ
    

### F-07 完了・納品

*   全タスク完了時、オーケストレータ最終判定
    
*   **一度だけ**リポジトリ作成→push
    
*   人間の最終承認を待って停止
    

### F-08 人間インターフェース（緊急ゲート）

*   仕様の過不足時：エージェント→人間へ質問
    
*   人間→エージェント：強制停止コマンド（状態保存・安全停止）
    

### F-09 設定管理

*   環境変数：`AGENT_ORCHESTRATOR_MODEL` / `AGENT_EXEC_MODEL` / `AGENT_QA_MODEL` / `AGENT_SURVEY_MODEL` / `GITHUB_TOKEN` / `PI_SESSION_DIR`
    
*   引数：`--orchestrator-model` 等で個別上書き。未指定は環境変数→デフォルトへフォールバック
    
*   モデル指定はpiの`--provider`/`--model`/`--thinking`と整合
    

### F-10 既存案件の現状把握

*   第2引数のコードベースをコンテナへマウント→調査エージェントがpiプロセス経由で解析→現状ドキュメント生成
    

### F-11 GitHubアップロード

*   全完了後一度だけ。token注入・認証はコンテナ内で管理
    

### F-12 役割別モデル割当（デフォルト）

*   オーケストレータ：Deepseek v4-flash系（コスパ）
    
*   実行：中〜高性能＋コスパ併用
    
*   QA：高性能（品質生命線）
    
*   調査：Deepseek v4-flash系（コスパ）
    

## 技術仕様

| 項目 | 仕様 |
| --- | --- |
| 言語/環境 | Python 3.x、uvで環境管理 |
| マネジメント層 | CrewAI（Agent/Task/Crew/Process） |
| 実行方式 | **Process.HIERARCHICAL**（オーケストレータ=manager） |
| 実行ツール | pi coding agent（CLIをsubprocessで呼ぶ） |
| pi連携 | **CrewAI Tool化**（実装・検証・調査Agentが共有） |
| コンテナ | Docker（ワークスペース隔離・冪等性確保） |
| モデル | OpenRouter、`AGENT_*_MODEL`で外部指定 |
| trust | `defaultProjectTrust`/`--approve`で非対話制御 |
| セッション | `-c`/`--fork`/`--session`で継続・分岐 |
| commit/push | commit=タスク毎、push=全完了後一度だけ |

### アーキテクチャ構造

```
CrewAI マネジメント層（Manager Only・コード非接触）
├─ オーケストレータ（manager）…… 戦略・タスク分解・最終判定
├─ 実行インターフェーサー ──> [piツール] ──> piプロセス（実装）
├─ QAクリティック ──> [piツール] ──> piプロセス（検証）
└─ 調査エージェント ──> [piツール] ──> piプロセス（既存コード把握）
```

### pi連携ツール（CrewAI Tool群）

*   `run_pi_single`：`pi -p @spec.md "タスク"` 形式でワンショット実行
    
*   `run_pi_continue`：`pi -c` / `--fork <id>` / `--session <id>` でセッション継続
    
*   `run_pi_fork`：失敗時・再実行時に`--fork`で新セッション生成
    
*   `compact_context`：`/compact`でコンテクスト解放
    
*   `export_session`：`/export`でJSONL化して永続化
    

### 制約・原則

**規約: 中間層の中立性（Manager-only principle）**

*   マネジメント層の全Agent（オーケストレータ・実行インターフェーサー・QAクリティック・調査）は、コード・ファイル・検証・調査に**直接アクセスしない**。
    
*   あらゆるコード接触（読む/書く/検証/調査）は、**piプロセスを介してのみ**行う。
    
*   調査Agentも、既存コードベースへのアクセスはpiプロセス経由に限定（例: `--tools read,grep,find,ls` の読み取り専用起動）。
    
*   マネジメント層は**自然言語のレポートのみを消費**し、不足情報はエージェントがpiプロセスへ質問して補完する。
    
*   この原則により、マネジメント層のコンテクストを軽量に保ち、コンテクスト肥大による速度低下・コスト増を防ぐ。
    

**その他の制約**

*   piはサブエージェント・MCP・プランモード・背景bashを内蔵しない→CrewAI側で複数エージェントを実装し、piは単一作業実行体として扱う
    
*   非対話モードはtrustプロンプト非表示→`defaultProjectTrust`/`--approve`で制御
    
*   モデル・trust・toolsは外部設定（環境変数/引数）
    
*   OpenRouter利用
    

## 画面仕様

*   CLIツール（`run-spec.sh`）＋ログモニタ（コンテナログtail）
    
*   状態は共有ボード（`plan.md`）＋タスク別ログで可視化
    

## 非機能要件

*   24/7稼働／コンテクスト肥大防止（compact・セッション分割）／冪等性／trust安全性／GitHub認証管理／費用最適化
    

## 決定事項ログ

1.  メタ＝純マネジメント層（コード/レビュー直接しない）
    
2.  複数エージェント協調（品質・分散・費用最適化）
    
3.  タスク粒度＝単一タスク＋対話型の融合
    
4.  報告＝自然言語出力＋質問補完
    
5.  人間介入＝質問提出＋強制停止のみ
    
6.  ワンライナー＝`run-spec.sh`、既存を第2引数でマウント
    
7.  GitHub＝commitタスク毎・push完了一回
    
8.  モデル割当＝外部設定（環境変数/引数）
    
9.  共有基盤＝単一ボード＋タスク分離。QAは完全独立
    
10.  費用考慮＝管理コスパ・検証高機能
     
11.  CrewAI実行＝Process.HIERARCHICAL
     
12.  pi連携＝CrewAI Tool化（QA・調査も独自にpiを呼ぶ）
     

## 保留事項

*   コンテナ内GitHub認証の詳細（トークン注入法・権限スコープ）
    
*   既存プロジェクトのマウント詳細（除外ファイル等）
    
*   OpenRouterの具体モデルID確定・各役割最終割当の実測調整